---
name: self-correction-loop
description: "ルーブリック駆動の自己修正ループを実行する: タスクに対して反復し、各試行を計測可能なルーブリックで確認し、独立した検証者サブエージェントがすべての基準の合格を確認したときにのみ完了を宣言する。ユーザーが明示的に「ループして」「反復して」「ヒルクライムして」と完了まで求めたとき — 例えば「テストが通るまでループして」「スコアが 100 になるまで反復して」「このベンチマークでヒルクライムして」「ルーブリックを満たすまで改善して」 — あるいは計測可能なゴールに向けた検証付き・ルーブリック駆動の反復を求めたときに必ず使うこと。"
---

# 自己修正ループ (Self-Correction Loop)

作業をループとして構造化する: 試行 → 計測 → 改善 → 繰り返し。そして
**独立した検証者**がすべての基準を満たしたと確認したときにのみ停止する。

独立性が重要な理由: 何かに取り組んだ後のあなたは、成果物そのものでは
なく自分の意図に引きずられており、自己批評は実際には終わっていない
作業を合格させがちである。フレッシュなコンテキストウィンドウを持つ
検証者サブエージェントは、ルーブリックと成果物だけを読み — あなたの
思考過程は決して読まない — その判定は実際にそこにあるものを反映する。

## ステップ 0: 作業場所を隔離する

cwd が git リポジトリなら、ループの作業は使い捨ての worktree の中で行う —
Claude Code の EnterWorktree、それが使えなければ CLI の `git worktree add`
で(CLI で作る worktree はリポジトリの隣に置く。例: `../<repo>-loop`)。
後述のフォールバックに落ちるのは、worktree による隔離そのものが
不可能か重すぎる場合だけであり、特定のツールが無いというだけでは落ちない。

- **dirty-state ガード(必須):** worktree はコミット済み状態から作られ、
  未コミットの変更は持ち込まれない。入る前に `git status` を確認する —
  未コミットの対象ファイルは worktree へのコピーを優先する(ユーザーの
  ブランチとインデックスに触れない)。WIP コミットでもよいが、ブランチの
  状態を変えてしまうため、コピーで済まない場合にのみ使う。
- **ベースライン照合(必須):** ステップ 2 のベースラインがユーザーの
  記述と食い違う場合(「失敗するはずのテスト」が通る)、まず古いベース
  からの分岐を疑う: 元チェックアウトの未コミット変更を確認する。
- worktree は新規チェックアウトである — `.env` も依存物も無い。計測
  コマンドを早期に一度実行し、環境起因の失敗には最小限のセットアップを
  行い、セットアップが重すぎる場合はフォールバックする。
- **フォールバック**(非 git / 重い環境 / worktree 不可): すべての
  ループ成果物(RUBRIC.md, EXPERIMENTS.md)は cwd 直下の `.loop/` に置き、
  git リポジトリでは `.git/info/exclude` に `.loop/` を追記する — ユーザー
  の .gitignore には触れない。既存の `.loop/` は上書きせず、報告してから扱いを決める。
  フォールバック時は以降のステップもこれを通して読む: ステップ 1 の
  ルーブリックは `.loop/` に置き、ステップ 3 の反復ごとのコミットは
  `EXPERIMENTS.md` のエントリで代替する — ユーザーが求めていない
  リポジトリを `git init` しない。

## ステップ 1: 作業を始める前にルーブリックを書く

ユーザーのゴールを、作業ディレクトリのルーブリックファイル `RUBRIC.md`
に変換する。すべての基準は客観的にチェック可能でなければならない —
コマンドの実行、または成果物の検査によって。曖昧な基準(「コードが
きれい」)は検証者にチェックするものを与えず、ループを早期に止めて
しまう。

- ゴールにすでにチェック可能なシグナル(テストスイート、スコアラー
  スクリプト、lint コマンド)が付いているなら、そのコマンドが
  ルーブリックの中核になる。正確なコマンドと要求される結果を記録する。
- 解くのではなくゲーミングによってゴールが満たされうる経路に対して、
  **ガード基準**を追加する:「テストファイルは改変されていない」
  「スコアラースクリプトは編集されていない」「期待出力はハードコード
  されていない」。合格へのループの圧力は、まさにこうした近道を生む
  圧力でもあるので、最初から名指ししておく。
- ゴールの一部が本質的に定性的なら、フレッシュな読み手が成果物だけから
  イエス/ノーで答えられるように言い換える(例:「`--help` に現れる
  すべての CLI フラグに README 内の説明がある」)。

## ステップ 2: ベースラインを確立する

まず誠実に 1 回試行し、計測可能なチェックを実行して結果を記録する。
ベースラインは、後の変更が本当に改善なのかを教えてくれ、ユーザーに
出発点がどこだったかを伝える。

## ステップ 3: 反復する

各反復で:

1. **変更は 1 つだけ。** 最も効果が見込めるアイデアを選ぶ。一度に
   複数の変更を入れてスコアが動いた場合、なぜ動いたのか分からなくなる。
2. **軽いチェックは自分で実行する**(テストコマンド、スコアラー)。
   すでに失敗すると分かっている試行に検証者の実行を浪費しない。
3. **実験を記録する**: `EXPERIMENTS.md` に、何を試したか、なぜか、
   計測結果、採用か差し戻しか。このログは失敗した実験の繰り返しを防ぎ、
   ユーザーにとって実行の窓になる。
4. **反復をコミットする。** worktree 内では 1 反復 = 1 コミットとする。
   コミットメッセージは、その反復の `EXPERIMENTS.md` エントリを要約する。
   コミットするのはタスクが変更すべきファイルだけ — `RUBRIC.md` と
   `EXPERIMENTS.md` はプロセス成果物であり、後のマージがユーザーの
   ブランチに持ち込まないよう、追跡させないままにしておく。

次に何を試すか選ぶとき: スカラーな微調整(定数の調整、リネーム、
並べ替え)は安価だがすぐに頭打ちになる。進捗が停滞したら、構造的な
賭けをする — 別のアルゴリズム、別の表現、問題の捉え直し — たとえ
一時的にスコアが後退しても。ログ上の停滞こそが、チューニングから
再構築へ切り替える合図である。

## ステップ 4: 独立に検証する

すべての基準が合格すると確信したら、Agent ツールで検証者サブ
エージェントを起動する。渡すのはルーブリックと成果物のパスだけ —
何をしたか、なぜ合格するはずかというあなたの要約は渡さない。
使えるテンプレート:

> `<path>/RUBRIC.md` のルーブリックを読んでください。各基準について、
> `<path>` 内の実際の成果物を確認し(基準がコマンドを指定している
> 場合は実行し)、判定を返してください: 基準ごとに PASS または FAIL、
> それぞれ具体的な根拠 1 行(コマンド出力、ファイル内容、行番号)
> 付きで。懐疑的であってください: あなたの仕事は成立して*いない*基準を
> 見つけることです。ファイルは一切変更しないでください。最後に総合
> 判定を出してください: すべての基準が合格した場合に限り PASS。

検証者がある基準を FAIL にしたら、その根拠を次の反復の入力として扱い、
ステップ 3 に戻る。判定に反論したり自分で再採点したりしない — 基準が
「未達成」ではなく「そもそも誤っている」ように見える場合は、黙って
落とすのではなくユーザーに報告する。

## ステップ 5: 停止する

- **成功:** 検証者がすべての基準に合格を出した。最終判定と、
  ベースラインから最終結果への改善を添えて報告する。worktree で作業した
  場合、引き渡すのは修正だけとする: ブランチ名、変更の diff 要約、
  プロダクト変更を取り込む正確な手順(ブランチをマージするか
  `git checkout <branch> -- <files>`)を報告する。既定は手順の提示で
  ある。自分で適用するのは、ユーザーの作業を失うことが証明可能に
  起こり得ない場合 — チェックアウトがクリーンであるか、diff により
  ユーザーの未コミット内容が適用後の内容に包含されると示せる場合 —
  に限り、どちらを行ったかを明示する。後片付けのコマンドは、ログを
  確認した後にユーザーが実行するものとして伝える:
  `git worktree remove --force <path>`(この `--force` は想定内 —
  未追跡のプロセス成果物を消すのがその役目)に続けてブランチを削除する。ユーザーに
  取り込みの経路を渡さずに成功を主張しない — 適用済みの修正か、
  ブランチ名と正確な取り込み手順のどちらかを必ず渡す — そして
  プロセス成果物をユーザーのチェックアウトに持ち込ませない。
- **予算:** ユーザーが反復回数や時間の予算を与えたなら、それに従う。
  与えられておらず、連続する複数の反復(少なくとも 1 回の構造的な
  賭けを含む)で進捗がない場合は停止し、それまでのベストの結果、
  実験ログ、追加予算があれば次に試すことを報告する。失敗または予算
  切れの場合、worktree はそのまま残し — 削除せず — そのパスを報告する。

フォールバック時は、`.loop/` が残っている旨とその消し方を 1 行添える。

このセッションでの検証者の合格判定なしに、決して成功を主張しない。
