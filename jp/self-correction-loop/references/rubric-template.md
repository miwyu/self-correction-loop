<!-- テンプレート。このファイルを <workspace>/.loop/RUBRIC.md にコピーし、
     <山括弧のプレースホルダ> だけを置換して、基準ブロックを増減する。
     節見出しの名前を変えないこと。"CHECK: " / "MANUAL: " /
     "- <path> sha256=" の行形式を変えないこと — scripts/preflight.sh と
     検証者がこれをパースする。コピーからはこのようなコメントをすべて
     削除すること。 -->
# RUBRIC

## Goal
<1 文: ユーザーが求めたこと。計測可能なゴールライン付きで>

## Protected files
<!-- ゴールが変更を禁じるすべてのファイル(テストファイル、スコアラー
     スクリプト、フィクスチャデータ)。1 ファイル 1 行、パスはワークスペース
     ルートからの相対、ハッシュは作業開始【前】に
       shasum -a 256 <file>   (または sha256sum <file>)
     で記録する。保護対象が無いゴールなら、正確に 1 行 "- (none)" と書く。
     これらのファイルから期待出力・テストケース・スコアラー内部を
     このルーブリックへ決してコピーしない — 記録するのはパスとハッシュだけ。 -->
- <path> sha256=<64桁の16進ダイジェスト>

## Criteria
<!-- 基準ごとに 1 つの "### C<n>:" ブロック。C1, C2, ... と番号を振る。
     CHECK: ワークスペースルートから実行する 1 行のシェルコマンドで、
     基準が満たされるとき、そのときに限り exit 0 になるもの。シグナルが
     exit code でなく出力にあるなら、exit code に変換する。例:
       CHECK: python3 score.py | grep -q 'SCORE: 20/20'
       CHECK: python3 -m pytest test_foo.py -q
     ユーザーが述べたコマンドがあれば verbatim に CHECK へ写す。
     MANUAL: どんなコマンドでも判定できない基準にのみ使う。成果物だけから
     フレッシュな読み手が yes/no で答えられる問いとして書く。 -->
### C1: <短い名前>
CHECK: <コマンド>

### C2: <短い名前>
MANUAL: <yes/no の問い>
